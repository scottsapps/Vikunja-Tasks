# Signing — fill in your Team ID and keep off git
Signing.xcconfig

# AI build-plan files
WATCH_APP_PLAN.md
CLAUDE.md

# Project skill master (canonical copy; re-zipped + re-uploaded to Claude via the Mac app)
skill/

# Personal Claude Code tooling (skill drift check, hooks)
.claude/

# ContextPool MCP server — per-machine session memory, not project code
ContextPool/

# App Store screenshot rig (personal tooling; not for forks)
screenshots/
VeyrnShots.*

# Xcode
*.xcuserstate
xcuserdata/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# Generated by xcodegen (`make gen`) — do NOT track. Xcode keeps rewriting these
# in its own scheme/project format on every open, which shows up as phantom git
# changes nobody made. project.yml is the source of truth; run `make gen` after
# cloning (or after any project.yml edit) to recreate them.
VikunjaWidget.xcodeproj/project.pbxproj
VikunjaWidget.xcodeproj/xcshareddata/

# macOS
.DS_Store
